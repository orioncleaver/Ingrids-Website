header:
  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about.html
  - titles:
      # @start locale config
      en      : &EN       Menu
      # @end locale config
    url: /menu.html
  - titles:
      # @start locale config
      en      : &EN       Location
      # @end locale config
    url: /location.html